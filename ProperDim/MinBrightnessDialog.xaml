<Window x:Class="ProperDim.MinBrightnessDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="ProperDim" Height="260" Width="300"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterOwner" ShowInTaskbar="False" ResizeMode="NoResize"
        Loaded="Window_Loaded">
	<WindowChrome.WindowChrome>
		<WindowChrome GlassFrameThickness="1" CornerRadius="0" CaptionHeight="0" UseAeroCaptionButtons="False" ResizeBorderThickness="0" />
	</WindowChrome.WindowChrome>
	<Border Background="#2D2D2D" BorderBrush="#3F3F46" BorderThickness="1" CornerRadius="8">
		<Grid>
			<Grid.RowDefinitions>
				<RowDefinition Height="30" />
				<RowDefinition Height="*" />
			</Grid.RowDefinitions>

			<Border Name="TitleBar" Grid.Row="0" Background="#1E1E1E" CornerRadius="7,7,0,0" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
				<Grid>
					<TextBlock Text="Adjust Minimum Brightness" Foreground="#AAAAAA" FontSize="12" FontWeight="SemiBold" VerticalAlignment="Center" Margin="10,0" IsHitTestVisible="False" />

					<StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
						<Button Content="✕" Width="45" Height="30" Background="Transparent" Foreground="White" BorderThickness="0" Click="Close_Click">
							<Button.Style>
								<Style TargetType="Button">
									<Setter Property="Template">
										<Setter.Value>
											<ControlTemplate TargetType="Button">
												<Border x:Name="ButtonBorder" Background="{TemplateBinding Background}" CornerRadius="0,7,0,0">
													<ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
												</Border>
												<ControlTemplate.Triggers>
													<Trigger Property="IsMouseOver" Value="True">
														<Setter TargetName="ButtonBorder" Property="Background" Value="#E81123"/>
													</Trigger>
												</ControlTemplate.Triggers>
											</ControlTemplate>
										</Setter.Value>
									</Setter>
								</Style>
							</Button.Style>
						</Button>
					</StackPanel>
				</Grid>
			</Border>

			<Grid Grid.Row="1" Margin="15" Background="Transparent" MouseWheel="MinSlider_MouseWheel">
				<Grid.RowDefinitions>
					<RowDefinition Height="Auto" />
					<RowDefinition Height="*" />
					<RowDefinition Height="Auto" />
					<RowDefinition Height="Auto" />
				</Grid.RowDefinitions>

				<TextBlock Grid.Row="0" Foreground="#FF909090" FontStyle="Italic" TextWrapping="Wrap" TextAlignment="Center" Margin="0,4,0,10">
					Adjust the slider below to set the absolute minimum brightness. 
				</TextBlock>

				<StackPanel Grid.Row="1" VerticalAlignment="Center" Margin="10,0,10,14">
					<StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,14">
						<TextBlock Text="Minimum Brightness: " Foreground="White" FontSize="14" FontWeight="SemiBold" />
						<TextBlock Text="{Binding ElementName=MinSlider, Path=Value, StringFormat={}{0:P0}}" Foreground="White" FontSize="14" FontWeight="SemiBold" />
					</StackPanel>

					<Grid>
						<Grid.ColumnDefinitions>
							<ColumnDefinition Width="*" />
							<ColumnDefinition Width="Auto" />
						</Grid.ColumnDefinitions>

						<Slider x:Name="MinSlider" Grid.Column="0" Margin="0,2,8,0" Minimum="0.00" Maximum="0.40" SmallChange="0.01" LargeChange="0.05" 
								ValueChanged="MinSlider_ValueChanged"
								PreviewMouseLeftButtonDown="MinSlider_PreviewMouseLeftButtonDown" 
								PreviewMouseMove="MinSlider_PreviewMouseMove" 
								PreviewMouseLeftButtonUp="MinSlider_PreviewMouseLeftButtonUp">
							<Slider.Style>
								<Style TargetType="{x:Type Slider}">
									<Setter Property="Cursor" Value="Hand" />
									<Setter Property="UIElement.SnapsToDevicePixels" Value="True" />
									<Setter Property="FrameworkElement.OverridesDefaultStyle" Value="True" />
									<Setter Property="Template">
										<Setter.Value>
											<ControlTemplate TargetType="{x:Type Slider}">
												<Grid Background="#00FFFFFF">
													<Border Name="TrackBackground" Background="#3E3E42" Height="6" CornerRadius="3" VerticalAlignment="Center" Margin="4,0" />
													<Track Name="PART_Track">
														<Track.DecreaseRepeatButton>
															<RepeatButton Command="Slider.DecreaseLarge">
																<RepeatButton.Template>
																	<ControlTemplate TargetType="{x:Type RepeatButton}">
																		<Border Background="Transparent" />
																	</ControlTemplate>
																</RepeatButton.Template>
															</RepeatButton>
														</Track.DecreaseRepeatButton>
														<Track.IncreaseRepeatButton>
															<RepeatButton Command="Slider.IncreaseLarge">
																<RepeatButton.Template>
																	<ControlTemplate TargetType="{x:Type RepeatButton}">
																		<Border Background="Transparent" />
																	</ControlTemplate>
																</RepeatButton.Template>
															</RepeatButton>
														</Track.IncreaseRepeatButton>
														<Track.Thumb>
															<Thumb Name="Thumb">
																<Thumb.Template>
																	<ControlTemplate TargetType="{x:Type Thumb}">
																		<Ellipse Fill="#B8B8B8" Width="18" Height="18" Stroke="#7E7E7E" StrokeThickness="2" Cursor="Hand" />
																	</ControlTemplate>
																</Thumb.Template>
															</Thumb>
														</Track.Thumb>
													</Track>
												</Grid>
											</ControlTemplate>
										</Setter.Value>
									</Setter>
								</Style>
							</Slider.Style>
						</Slider>

						<Button x:Name="PreviewButton" Grid.Column="1" Content="👁" Width="24" Height="20" Background="#3E3E42" Foreground="#5777a3" BorderThickness="1" BorderBrush="#555"
								PreviewMouseLeftButtonDown="PreviewButton_PreviewMouseLeftButtonDown"
								PreviewMouseLeftButtonUp="PreviewButton_PreviewMouseLeftButtonUp"
								MouseLeave="PreviewButton_MouseLeave">
							<Button.Template>
								<ControlTemplate TargetType="{x:Type Button}">
									<Border Name="border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="2">
										<ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
									</Border>
									<ControlTemplate.Triggers>
										<Trigger Property="IsMouseOver" Value="True">
											<Setter TargetName="border" Property="Background" Value="#4E4E52" />
											<Setter TargetName="border" Property="BorderBrush" Value="#6D6D72" />
											<Setter Property="Foreground" Value="White" />
										</Trigger>
										<Trigger Property="IsPressed" Value="True">
											<Setter TargetName="border" Property="Background" Value="#2D2D2D" />
										</Trigger>
									</ControlTemplate.Triggers>
								</ControlTemplate>
							</Button.Template>
						</Button>
					</Grid>
				</StackPanel>

				<TextBlock Grid.Row="2" Foreground="#FF909090" FontStyle="Italic" TextWrapping="Wrap" TextAlignment="Center" Margin="0,0,0,16">
					TIP: Setup your hotkeys before saving changes here so you can't accidentally blackout your screen.
				</TextBlock>

				<UniformGrid Grid.Row="3" Columns="2" HorizontalAlignment="Center" Margin="0,0,0,5">
					<UniformGrid.Resources>
						<Style TargetType="{x:Type Button}">
							<Setter Property="Foreground" Value="#FFFFFFFF" />
							<Setter Property="BorderBrush" Value="#555555" />
							<Setter Property="BorderThickness" Value="1" />
							<Setter Property="Template">
								<Setter.Value>
									<ControlTemplate TargetType="{x:Type Button}">
										<Border Name="border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="0">
											<ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
										</Border>
										<ControlTemplate.Triggers>
											<Trigger Property="IsMouseOver" Value="True">
												<Setter TargetName="border" Property="Background" Value="#4E4E52" />
												<Setter TargetName="border" Property="BorderBrush" Value="#6D6D72" />
											</Trigger>
											<Trigger Property="IsPressed" Value="True">
												<Setter TargetName="border" Property="Background" Value="#2D2D2D" />
											</Trigger>
										</ControlTemplate.Triggers>
									</ControlTemplate>
								</Setter.Value>
							</Setter>
						</Style>
					</UniformGrid.Resources>
					<Button Content="Looks good" Width="100" Height="25" Background="#3E3E42" Margin="5,0" Click="LooksGood_Click" />
					<Button Content="Never mind" Width="100" Height="25" Background="#3E3E42" Margin="5,0" Click="NeverMind_Click" />
				</UniformGrid>
			</Grid>
		</Grid>
	</Border>
</Window>